# Unity 가 자동으로 만드는 것들 — 올리지 않는다 (다른 컴퓨터에서 다시 만들어진다)
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Mm]emoryCaptures/
[Rr]ecordings/

# 비주얼스튜디오·라이더가 만드는 것
.vs/
.vscode/
.idea/
*.csproj
*.unityproj
*.sln
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# 빌드 결과물
*.apk
*.aab
*.app
*.unitypackage
sysinfo.txt
crashlytics-build.properties

# ★모델링 작업도 **전부 올린다** (2026-08-12 사용자 "전부다 올려").
#   ref/ 의 CC0 번들(97MB)은 다시 받으려면 어디서 받았는지부터 찾아야 한다 —
#   「다시 만들 수 있다」와 「다시 만들기 쉽다」는 다르다.
#   ☆`.blend1` 만 뺀다 — 블렌더가 저장할 때마다 만드는 **직전 판 백업**이라 쓸모가 없다
*.blend1

# ★옛 장난감 펫 108개 (53MB) — **올린다** (2026-08-12 사용자 "다른 컴퓨터에서 열면
#   바로 할 수 있게, 적용 안 되는 거 없이"). `Boot` 의 동행 펫과 `ToyShowcase`(F4 진열)가
#   실제로 읽으므로, 빼 두면 다른 컴퓨터에서 **상자로 나오고 진열이 빈다.**
#   ☆제일 큰 파일이 3.1MB 라 LFS 없이 그냥 올라간다 (잰 값)


# (확인용 스크린샷도 올린다 — 2026-08-12 "전부다 올려")

# 렉 진단 기록 — 굴러가면서 생기는 것이라 안 올린다
렉_스파이크.txt
렉_범인찾기.txt
