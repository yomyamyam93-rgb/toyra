using UnityEngine;

/// **이것과 그 자손에는 검은 테두리를 두르지 않는다.**
///
/// ★★왜 이름 목록이 아니라 표식인가 (2026-08-05 사용자 "이 나무들 스타일라이즈드,
///   이거 외곽선 들어가면 안돼고"):
///   `Outliner.건너뛰기` 는 **이름**으로 거른다 — "나무" · "잎" · "줄기".
///   그런데 실제로 씬에 서 있는 것은 `tree_stylized_4(Clone)` 안의 `Canopy` · `Cone` 이라
///   한 글자도 안 걸렸다. 모델을 새로 받을 때마다 이름이 달라지므로 목록으로는 못 따라간다.
///   → **붙이는 쪽이 정한다.** `WorldGen` 이 환경 프리팹을 심을 때 이 표식을 달면,
///     모델 이름이 무엇이든 테두리가 안 붙는다.
///
/// ★규칙 자체는 그대로다: 테두리는 **살아 있는 것과 사람이 놓은 것**에만 두른다.
///   풀·나무·땅에 검은 선이 생기면 화면이 그물처럼 지저분해진다.
public class NoOutline : MonoBehaviour { }
